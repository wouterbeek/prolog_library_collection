This directory contains copies of libraries from the SWI-Prolog
standard libraries that are tweaked in order to support the extended
HTTP libraries that are provided by the Prolog Library Collection.
